<<<<<<< HEAD
name: simple CI pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run a simple command     
        run: echo "GitHub Actions."

      - name: step 1
        run: echo "This is step 1."

      - name: step 2
        run: echo "This is step 2." 

      - name: step 3
        run: echo "This is step 3."
      
      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin 

      - name: Docker Build 
        run: docker build -t ghcr.io/${{ github.actor }}/devops:v1 .
      
      - name: Docker Push
        run: docker push ghcr.io/${{ github.actor }}/devops:v1
=======
name: Simple CI Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run a simple command
        run: echo "This is my pipeline run"

      - name: step1
        run: echo "this is my first step1"


>>>>>>> 2645f0e3385c211c7e56a59d16f40c73d3aeef3c
